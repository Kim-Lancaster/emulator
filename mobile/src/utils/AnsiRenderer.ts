// Utility for rendering ANSI escape sequences
// Since WebView handles rendering, this is a placeholder

export class AnsiRenderer {
  static render(text: string): string {
    // TODO: Parse and render ANSI codes if needed
    return text;
  }
}